<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bookworld">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ElnazMohajerAnsari_BookWorld">

        <activity
            android:name=".activities.WatchListActivity"
            android:exported="false"
            android:parentActivityName=".activities.GenreActivity" />
        <activity
            android:name=".activities.FavoriteListActivity"
            android:exported="false"
            android:parentActivityName=".activities.GenreActivity" />
        <activity
            android:name=".activities.SearchActivity"
            android:exported="false"
            android:parentActivityName=".activities.GenreActivity" />
        <activity
            android:name=".activities.ReviewActivity"
            android:exported="false"
            android:parentActivityName=".activities.BookDetailsActivity" />
        <activity
            android:name=".activities.BookDetailsActivity"
            android:exported="false"
            android:parentActivityName=".activities.BookActivity" />
        <activity
            android:name=".activities.BookActivity"
            android:exported="false"
            android:parentActivityName=".activities.GenreActivity" />
        <activity
            android:name=".activities.GenreActivity"
            android:exported="false" />
        <activity
            android:name=".activities.SignUpActivity"
            android:exported="false"
            android:parentActivityName=".activities.LoginActivity" />
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>